import React from 'react';
import { Card, CardBody } from '../ui/Card';

export default function MetricsPage() {
  return (
    <div className="space-y-4">
      <Card>
        <CardBody>Metrics table/cards will render here (bar coverage, tickets, health).</CardBody>
      </Card>
    </div>
  );
}
