# ORR gating / dynamic window
ORR_MIN_ATR_TICKS=8
ORR_MIN_OR_WIDTH_TICKS=6
ORR_WINDOW_MIN_MINUTES=10
ORR_WINDOW_MAX_MINUTES=30
ORR_CONTINUATION_MAX_TRADES=1

# Presentation / exposure flags
ORR_EXPOSE_CONTINUATION=true
ORR_EXPOSE_ATR_METRICS=true

# Baseline risk per trade (USD)
RISK_PER_TRADE_USD_DEFAULT=50
