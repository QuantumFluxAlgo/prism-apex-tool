services:
  api:
    command: ["node","apps/api/start-runtime.cjs"]
  dashboard-lite:
    ports:
      - "5178:80"
services:
  dashboard-lite:
    build:
      context: .
      dockerfile: apps/dashboard-lite/Dockerfile
