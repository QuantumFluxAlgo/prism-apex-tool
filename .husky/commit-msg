#!/usr/bin/env bash
set -euo pipefail
if command -v pnpm >/dev/null 2>&1 && pnpm exec --silent which commitlint >/dev/null 2>&1; then
  pnpm exec commitlint --edit "$1"
else
  echo "[husky] commitlint not installed; skipping commit message lint"
fi
