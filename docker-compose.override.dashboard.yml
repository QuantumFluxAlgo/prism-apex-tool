services:
  dashboard:
    environment:
      - API=http://api:3000
      - CI=1
    command: sh -lc "corepack enable && corepack prepare pnpm@9.0.0 --activate && pnpm install --no-frozen-lockfile && pnpm -C apps/dashboard dev --host 0.0.0.0 --port 5180"
