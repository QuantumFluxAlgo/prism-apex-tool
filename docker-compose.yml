# API-only compose for local Docker runs.
name: prism-apex-tool
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: prism-apex-tool-api:latest
    environment:
      NODE_ENV: production
      LOG_LEVEL: info
      TRUST_PROXY: "true"
      DATA_DIR: /data
      BEARER_TOKEN: ""
    ports:
      - "3000:3000"
    volumes:
      - api-data:/data
    working_dir: /app
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s

volumes:
  api-data:
