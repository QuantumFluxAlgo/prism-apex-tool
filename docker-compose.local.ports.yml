services:
  api:
    restart: unless-stopped
    ports:
      - "3000:3000"
  dashboard-full:
    restart: unless-stopped
    ports:
      - "5180:80"   # serve UI on 5180
    # intentionally DO NOT publish 8080 here
  ingress-yahoo:
    restart: unless-stopped
    ports:
      - "8080:8080" # free now that dashboard doesn't map it
  gapfill-cron:
    restart: unless-stopped
